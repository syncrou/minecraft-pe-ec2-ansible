---
# MinecraftPE MP ec2 
- hosts: localhost
  gather_facts: False
  vars:
  - key: db
  - name: minecraftpe-mp
  - instance_type: m1.small
  - security_group: sg-dfe6c6a6
  - image: ami-bcb853d1
  - region: us-east-1
  - subnet: subnet-f849ff96
  tasks:
  - name: Create Ec2 Instance
    local_action:
      module: ec2
      key_name: "{{ key }}"
      instance_tags: {Name: "{{ name }}"}
      group_id: "{{ security_group }}"
      instance_type: "{{ instance_type }}"
      region: "{{ region }}"
      image: "{{ image }}"
      wait: yes
      count: 1
      vpc_subnet_id: "{{ subnet }}"
      assign_public_ip: yes
    register: ec2 

  - name: Wait for port 22 to be ready
    #local_action: wait_for port=22 host={{ ansible_ssh_host }}  search_regex=OpenSSH delay=10
    local_action: wait_for: host="{{ item.public_dns_name }}" port=22 delay=60 timeout=320 state=started
    #- name: Echo the IP Address
    #local_action: shell: ifconfig | grep inet
    #- debug: msg="the output was {{ ec2.stdout }}"
